#include "container.h"
#include <stdio.h>

#define TAM_PRINCIPAL 10

int main()
{
    // Inicializa uma quantidade definida de estruturas "secundárias" (Container)
    Container *containeres[TAM_PRINCIPAL] = NULL;
}